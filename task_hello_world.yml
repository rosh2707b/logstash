---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}
inputs:
  - name: resource-dmr

run:
  path: /opt
  args: 
    - -exc
    - |
      cd resource-dmr
      ls -ltr
