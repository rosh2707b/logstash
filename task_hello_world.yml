---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}
inputs:
  - name: resource-dmr

run:
  path: /bin/sh
  args: 
    - -exc
    - |
      ls -ltr
      cd resource-dmr
      ls -ltr
      cd ..
      mkdir dmr
      cd resource-dmr
      cp resource-dmr/README.md dmr/
      cd dmr
      ls -ltr
