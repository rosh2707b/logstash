---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}
inputs:
  - name: resource-dmr

run:
  path: /bin/sh
  args: 
    - -exc
    - |
      cd resource-dmr
      ls -ltr
      cd ..
      ls -ltr
      mkdir dmr
      ls -ltr
      cp resource-dmr/*.ksh /dmr
      
