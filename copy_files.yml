---
platform: linux

image_resource:
  type: docker-image
  source: {repository: alpine}

inputs:
  - name: resource-dmr

run:
  path: /bin/sh
  args: 
    - -exc
    - |
      pwd
      ls -ltr
      cd resource-dmr
      ls -ltr
      cd ..
      mkdir dmr
      ls -ltr
      cp resource-dmr/README.md dmr/
      cp dmr/README.md dsclagnt-ch2-a1s.sys.comcast.net:/home/rboddu495/
